include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - web/**
    - node_modules/**
    - dist/**
    - android/**

linter:
  rules:
    # avoid_print: false
    # prefer_single_quotes: true
