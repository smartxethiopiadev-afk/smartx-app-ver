name: smart_learn_ethiopian
description: "Smart Learn Ethiopian - Comprehensive Ethiopian Curriculum Learning & Exam Preparation Platform for Grades 9-12 with curriculum notes, quizzes, worksheets, offline study, and progress tracking."
publish_to: 'none'

version: 1.0.16+16

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Visual assets & Fonts
  google_fonts: ^6.2.0

  # Local storage & Connectivity
  shared_preferences: ^2.2.3
  path_provider: ^2.1.2
  http: ^1.2.0
  carousel_slider: ^5.0.0
  connectivity_plus: ^6.0.3
  device_info_plus: ^10.1.0

  # Backend Integration (Supabase Database & Storage)
  supabase_flutter: ^2.6.0

  # Sharing & Utilities
  url_launcher: ^6.3.0
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.1
  dependency_validator: ^3.2.3
  flutter_lints: ^3.0.0

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/app_logo.png"
  min_sdk_android: 21
  web:
    generate: false
    background_color: "#000000"
    theme_color: "#000000"

flutter_native_splash:
  color: "#000000"
  fullscreen: true
  android_12:
    color: "#000000"
    icon_background_color: "#000000"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
